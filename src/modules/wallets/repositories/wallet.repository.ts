/**
 * @deprecated Import from infrastructure/persistence instead.
 * Kept for backward compatibility — external modules still import WalletRepository from here.
 */
export { WalletRepositoryImpl as WalletRepository } from '../infrastructure/persistence/wallet.repository.impl';
