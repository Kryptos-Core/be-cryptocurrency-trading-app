/**
 * Phase 2 — TON (TonConnect, cell-based txs, jettons).
 * Register in BlockchainProviderFactory when deposit/withdraw/link for TON is implemented.
 */
export const TON_PROVIDER_PHASE2_PLACEHOLDER = true;
