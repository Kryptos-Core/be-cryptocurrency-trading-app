# matching-engine (shadow)

Scaffold cho Phase 6:

- nhận order stream shadow
- không ghi production `wallets`/`orders`/`trades`
- xuất kết quả shadow để parity compare

TS runtime đã có `MATCHING_ENGINE=go_shadow` để enqueue shadow job placeholder.
